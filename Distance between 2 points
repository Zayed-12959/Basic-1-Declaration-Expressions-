Calculate distance between two points

Write a C program to calculate the distance between two points.
Test Data :
Input x1: 25
Input y1: 15
Input x2: 35
Input y2: 10
Expected Output:
Distance between the said points: 11.1803

------------------------------------------------------------------

#include<stdio.h>
#include<math.h>

int main(){

    int x1,y1,x2,y2;
    printf("x1: ");
    scanf("%d", &x1);
    printf("y1: ");
    scanf("%d", &y1);
    printf("x2: ");
    scanf("%d", &x2);
    printf("y2: ");
    scanf("%d", &y2);

    float distance = sqrt(pow((float)x2-(float)x1, 2) + pow((float)y2-(float)y1, 2));

    printf("Distance between 2 points: %.2f", distance);

return 0;
}
